version: "3.8"

services:
  timemula-ui:
    image: ghcr.io/adnanertorer/timemula-ui:latest
    container_name: timemula-ui
    restart: always
    ports:
      - "4200:80"
    networks:
      - timemula-net

networks:
  timemula-net:
    driver: bridge
